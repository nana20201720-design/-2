# 🚀 دليل تحديث اللعبة على GitHub و APK أوتوماتيكياً

هذا الدليل مخصص لك لتسهيل عملية تحديث الكود الخاص بك وضمان أن ملف الـ APK يتم تحديثه تلقائياً للمستخدمين.

---

## 🛠️ الخطوات الأساسية لتحديث الكود (Git Commands)

عندما تنتهي من تعديل أي شيء في الكود هنا في AI Studio وتريد إرساله إلى GitHub، اتبع هذه الأوامر الثلاثة (المقدسة) في الـ Terminal:

### 1️⃣ تجهيز الملفات (Add)
هذا الأمر يخبر Git بأنك تريد تضمين جميع التغييرات الجديدة.
```bash
git add .
```

### 2️⃣ تسجيل التعديلات (Commit)
هذا الأمر يحفظ التغييرات محلياً مع رسالة تصف ما قمت به (استبدل النص بين القوسين بما تريد).
```bash
git commit -m "إضافة مميزات جديدة وتحديث الأيقونات"
```

### 3️⃣ الرفع إلى السحاب (Push)
هذا الأمر يرسل كل شيء إلى مستودعك على GitHub. بمجرد تنفيذ هذا الأمر، ستبدأ عملية بناء الـ APK تلقائياً إذا كنت تستخدم GitHub Actions.
```bash
git push origin main
```

---

## 📱 كيف يتم تحديث الـ APK تلقائياً؟

بما أن مشروعك مربوط بـ **GitHub Actions**، فبمجرد عمل `git push`:
1. سيبدأ GitHub في "طبخ" نسخة جديدة من التطبيق في الخلفية.
2. يمكنك متابعة العملية من تبويب **Actions** في صفحة مستودعك على GitHub.
3. عند انتهاء البناء، سيظهر ملف الـ APK الجديد في قسم **Releases** أو **Artifacts**.

---

## 🌐 حالة الأونلاين (Online Status)
*   **Firebase:** نظام قواعد البيانات (Firestore) مفعل وشغال تماماً.
*   **المزامنة:** بيانات اللاعبين (الذهب، اللفل، التخصيص) يتم حفظها ومزامنتها لحظياً.
*   **الغرف:** نظام اللعب الجماعي مهيأ للعمل بكفاءة في نسخة الـ APK.

---

## 💡 نصائح للمحترفين:
*   **تغيير الإصدار:** قبل كل تحديث كبير، يفضل تغيير رقم الإصدار في ملف `package.json` ليعرف اللاعبون أن هناك نسخة جديدة.
*   **تنبيه التحديث:** اللعبة الآن مبرمجة لتظهر رسالة للمستخدم (Update Available) بمجرد توفر نسخة جديدة على السيرفر.

---
**تم إعداد هذا الدليل لمساعدتك في إدارة مشروعك باحترافية!** 🎖️
